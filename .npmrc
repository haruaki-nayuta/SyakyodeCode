# サプライチェーン攻撃対策: 公開から 3 日経っていないバージョンはインストール禁止
# 単位は分（3 日 = 4320 分 / 7 日 = 10080 分）
minimum-release-age=4320

# 厳格な依存ツリー（暗黙の依存に頼らない）
strict-peer-dependencies=true

# postinstall スクリプトはデフォルト拒否し、package.json の pnpm.onlyBuiltDependencies で明示許可したものだけ実行
# （pnpm 10+ の既定動作だが、念のため明示）
ignore-scripts=false
